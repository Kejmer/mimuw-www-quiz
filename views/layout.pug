doctype html
html
  head
    meta(charset="UTF-8")
    title QuizIT
    link(rel='stylesheet', href='/stylesheets/main.css')
    link(rel='stylesheet', href='/stylesheets/' + css_file + '.css')
  body
    nav#home
      a#dashboard(href="/") główna
      if !logged_in
        a#login(href="/login") zaloguj
      else
        a#logout(href="/logout") wyloguj
        a#change_password(href="/change_password") zmien hasło
    block content
