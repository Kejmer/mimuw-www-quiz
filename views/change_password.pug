extends layout

block content
  form(method='POST' action='/change_password')
    input(type="hidden" name="_csrf" value=csrfToken)
    div.form-group
      label(for='price') Nowe hasło:
      br
      input#new_password.form-control(type='password', name='password')
    button(type='submit') ZMIEŃ TO
